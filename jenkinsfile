pipeline {
    agent any

    stages {
        stage('main') {
            steps {
                sh 'build go'
            }
        }
 stage('test') {
            steps {
                sh 'test go'
            }
        }
    }
}
